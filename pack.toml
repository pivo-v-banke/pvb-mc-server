# Default template for `pivo-cli create-space <name>` (without --from-pack).
# Safe to edit or replace; copied into each new space as initial pack.toml.

[pack]
name = "template"
minecraft_version = "1.21.1"
loader = "fabric"
loader_version = "0.16.0"

mods = []
